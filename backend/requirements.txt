# AI Indoor Navigation Assistant — Backend Dependencies
# Install with:
#   pip install -r requirements.txt

# CPU-only PyTorch wheels (dramatically reduces RAM usage vs default CUDA build)
--extra-index-url https://download.pytorch.org/whl/cpu

Flask==3.1.3
Flask-Cors==6.0.5
ultralytics==8.4.104
torch==2.7.0+cpu
torchvision==0.20.1+cpu
opencv-python-headless==4.11.0.86
gunicorn==21.2.0
Pillow==10.4.0
